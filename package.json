{
  "name": "waynebphoto",
  "version": "1.0.0",
  "private": true,
  "description": "Wayne B Photography — events site with Drive-backed client galleries",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types test/parse-listing.test.mjs && node --experimental-strip-types test/drive-gallery.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.10"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0"
  }
}
